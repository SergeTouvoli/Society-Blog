document.addEventListener("DOMContentLoaded",(function(){const password=document.querySelector("#userPassword");""!==password.value.length&&verifFormatPassword(password),password.addEventListener("keyup",(function(){verifFormatPassword(password)})),password.addEventListener("focus",(function(){verifFormatPassword(password)}))}));